{
    "configurations": [
        {
            "name": "Docker Node.js Launch",
            "type": "docker",
            "request": "launch",
            "preLaunchTask": "docker-run: debug",
            "platform": "node",
            "node": {
                "package": "${workspaceFolder}/vendor/symfony/stimulus-bundle/assets/package.json",
                "localRoot": "${workspaceFolder}/vendor/symfony/stimulus-bundle/assets"
            }
        }
    ]
}
<form method="post" action="{{ path('app_order_mission_delete', {'id': order_mission.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ order_mission.id) }}">
    <button class="btn">Delete</button>
</form>

{% extends 'base.html.twig' %}

{% block title %}TypeExpense{% endblock %}

{% block body %}
    <h1>TypeExpense</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_expense.id }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ type_expense.code }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ type_expense.name }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ type_expense.status }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_type_expense_index') }}">back to list</a>

    <a href="{{ path('app_type_expense_edit', {'id': type_expense.id}) }}">edit</a>

    {{ include('type_expense/_delete_form.html.twig') }}
{% endblock %}

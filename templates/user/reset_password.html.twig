{% extends 'base.html.twig' %}

{% block title %}Reset Password{% endblock %}

{% block body %}
    <h1>Reset Password</h1>
    <form method="post">
        <input type="password" name="password" placeholder="Enter new password" required>
        <button type="submit">Reset Password</button>
    </form>
{% endblock %}


{% extends 'base.html.twig' %}

{% block title %}Réinitialiser le mot de passe{% endblock %}


{% block body %}
    <div class="col-12">
        {{ form_start(form) }}
        <div class="card card-fluid">
            <div class="card-header bg-soft-custom">
                <h6>Réinitialiser le mot de passe</h6>
            </div>

            {% extends 'base.html.twig' %}

            {% block title %}Réinitialiser le mot de passe{% endblock %}


            {% block body %}
                <div class="col-12">

                    <div class="card card-fluid">
                        <div class="card-header bg-soft-custom">
                            <h6>Réinitialiser le mot de passe</h6>
                        </div>

                        <div class="card-footer">
                            <div class="card-footer-content">
                                <button type="submit" class="btn btn-primary float-right ml-2">Réinitialiser</button>
                                <a href="{{ path('app_user_index') }}" class="btn btn-info float-right"><i class="oi oi-home"></i> Retour</a>
                            </div>
                        </div>
                    </div>

                </div>
            {% endblock %}

            <div class="card-footer">
                <div class="card-footer-content">
                    <button type="submit" class="btn btn-primary float-right ml-2">Réinitialiser</button>
                    <a href="{{ path('app_user_index') }}" class="btn btn-info float-right"><i class="oi oi-home"></i> Retour</a>
                </div>
            </div>
        </div>
        {{ form_end(form) }}
    </div>
{% endblock %}

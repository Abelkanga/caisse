{% extends 'base.html.twig' %}

{% block title %}User index{% endblock %}

{% block body %}
    <div class="col-12 col-md-12">
        <div class="card">
            <div class="card-header bg-soft-custom">
                Liste des utilisateurs
            </div>
            <div class="card-body">
                <div class="table-responsive">
                    <table class="table table-striped table-hover bordered table-sm basic-datatable">
                        <thead class="thead-light">
                        <tr>
                            <th>Nom & Prénoms</th>
                            <th>Identifiant</th>
                            <th>Contact</th>
                            <th>Email</th>
                            <th>Fonction</th>
                            <th>Service</th>
                            <th>Rôle</th>
                            <th>Action</th>
                        </tr>
                        </thead>
                        <tbody>
                        {% for u in users %}
                            <tr>
                                <td class="align-middle">{{ u.fullName }} {{ u.prenom }}</td>
                                <td class="align-middle">{{ u.pseudo }}</td>
                                <td class="align-middle">{{ u.contact }}</td>
                                <td class="align-middle">{{ u.email }}</td>
                                <td class="align-middle">{{ u.fonction }}</td>
                                <td class="align-middle">{{ u.emeteur }}</td>
                                <td class="align-middle">
                                    {% if u.roles is not empty %}
                                        {{ u.roles[0]|replace({'ROLE_': ''}) }}
                                    {% endif %}
                                </td>
                                <td class="text-center align-middle">
                                    <a class="btn btn-sm btn-icon btn-secondary" data-toggle="tooltip" data-html="true" title="Modifier" href="{{ path('app_user_edit', {'id': u.id}) }}">
                                        <i class="fa fa-pencil-alt"></i>
                                    </a>
                                </td>
                            </tr>
                        {% endfor %}
                        </tbody>
                    </table>

                </div>
            </div>
            <div class="card-footer">
                <div class="card-footer-content">
                    <a href="{{ path('app_user_new') }}" class="btn btn-primary float-right ml-2">
                        Nouveau</a>
                        <a href="{{ path('app_welcome') }}" class="btn btn-success float-right ml-3">
                        <i class="oi oi-home"></i>
                        Accueil</a>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
